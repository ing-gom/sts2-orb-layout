# Godot 4+
.godot/
*.translation

# .NET / Rider / VS / Claude Code
bin/
obj/
.idea/
.vs/
.claude/
*.user
*.suo

# Local-only build overrides
local.props

# Logs / runtime artifacts
*.log

# Release zips at repo root (built by `dotnet build` + zip)
/Sts2OrbLayout-v*.zip
